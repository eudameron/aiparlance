import type { AipDocument } from "@aiparlance/parser";

/** Emit TypeScript interfaces/guards from a validated AST (M5). */
export function emitTypeScript(_doc: AipDocument): string {
  throw new Error("TypeScript emitter not implemented (Phase C / M5)");
}
