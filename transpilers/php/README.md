# @aiparlance/php

PHP classes with typed public properties for each entity (`Entity` / `EntityCreate` / `EntityUpdate`).

```ts
import { emitPhp } from "@aiparlance/php";

process.stdout.write(emitPhp(doc));
```

Status: **MVP Preview**.
