# @aiparlance/tests

Vitest-style CRUD case fixtures per entity with a `crud` declaration.

```ts
import { emitTests } from "@aiparlance/tests";

process.stdout.write(emitTests(doc));
```

Status: **MVP Preview**.
