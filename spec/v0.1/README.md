# AI Parlance v0.1

Normative artifacts for version **0.1**.

## Files

| File | Description |
|---|---|
| [grammar.ebnf](grammar.ebnf) | Core grammar (summary) |

## Stability

| Level | Blocks |
|---|---|
| Core | `app`, `entity`, `crud`, inline validation; inline `enum(…)` and `belongs_to` |
| Infra | `index`, `seed`, `timestamps`, `soft_delete`, migrations |
| Security (beta) | `auth`, `policy`, predicates |
| Behavior (beta) | `workflow`, `event`, `lifecycle`, `job`, `queue`; workflow statements (`emit`, …) |

Full prose: [Specification](https://docs.aiparlance.org/en/specification) (EN) · [Especificação](https://docs.aiparlance.org/pt/specification) (PT).
