# Specification

Normative definition of the AI Parlance language.

| Version | Path | Status |
|---|---|---|
| **0.1** | [v0.1/](v0.1/) | Draft (Core stable within v0.1; reference toolchain **preview** in monorepo) |

Human-readable specification: **https://docs.aiparlance.org** ([`docs/`](../docs/), EN/PT). This directory holds machine-oriented artifacts:

- EBNF grammar
- Future: JSON Schema, parser tests, conformance suite

## Examples

Reference specs: [`examples/`](../examples/). CI validates Core-tier parse/validate for every `.aip` file; see [`ROADMAP.md`](../ROADMAP.md).
