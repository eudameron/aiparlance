---
title: Emitter PostgreSQL
description: "@aiparlance/sql — papel schema, nota 15/23, objetivos pass/partial/fail"
---

# Emitter PostgreSQL

| | |
|---|---|
| Pacote | `@aiparlance/sql` |
| CLI | `aip emit sql` |
| Papel | `schema` |
| **Nota** | **15/23** (65%) |
| Faixa | Fatia forte |
| Scorecard | [transpilers/sql/EMITTER_OBJECTIVES.md](https://github.com/eudameron/aiparlance/blob/main/transpilers/sql/EMITTER_OBJECTIVES.md) |

Avaliado pelo checklist mestre ([55 IDs](https://github.com/eudameron/aiparlance/blob/main/EMITTER_OBJECTIVES.md), v2 · 2026-08-06).

## Resumo

| ✅ Pass | ⚠️ Partial | ❌ Fail | ➖ N/A |
|---|---|---|---|
| 15 | 1 | 7 | 32 |

Itens N/A estão fora do papel `schema` e não entram no denominador da nota.

## O que produz

DDL PostgreSQL: `CREATE TABLE`, índices, seeds. Dialeto SQL primário.

## ✅ Passou

| ID | Objetivo |
|---|---|
| `A1` | Emit entity shapes (types / structs / schemas / tables) from `entity` |
| `A4` | Emit enums / constrained variants from `enum(…)` |
| `A5` | Emit `belongs_to` as FKs or refs |
| `A6` | Emit implicit `id` primary key |
| `A7` | Emit `timestamps` (`created_at` / `updated_at`) |
| `A8` | Emit `soft_delete` field/column (`deleted_at`) |
| `B1` | Honor `required` / `optional` |
| `B2` | Honor `unique` |
| `C1` | Durable schema (DDL or ORM / query models) |
| `C2` | Indexes from `index { }` |
| `C3` | Seeds from `seed { }` |
| `C6` | Respect `app.database` dialect |
| `H3` | Golden / CI for `minimal.aip` (or dialect twin) |
| `H4` | Emit succeeds on matching full-tier examples without crash |
| `H5` | Naming aligned with docs (plural tables, `*_id`, snake_case) |

## ⚠️ Parcial

| ID | Objetivo |
|---|---|
| `B4` | Map semantic types (`email`, `phone`, …) distinctly |

## ❌ Ainda falhando (aplicáveis)

| ID | Objetivo |
|---|---|
| `A9` | Soft-delete **semantics** (default reads filter deleted rows) |
| `B3` | Apply `validation { }` beyond required folding |
| `C4` | Versioned migrations (ordered **up**) |
| `C5` | Migration **down** / rollback |
| `C7` | Transactions for multi-statement / workflow writes |
| `H1` | Human-readable API / domain documentation |
| `H2` | Automated test fixtures or scaffolds |

Scorecard completo (incluindo ➖ N/A): [transpilers/sql/EMITTER_OBJECTIVES.md](https://github.com/eudameron/aiparlance/blob/main/transpilers/sql/EMITTER_OBJECTIVES.md).

## Testes

Vitest do pacote + golden `transpilers/sql/fixtures/minimal.sql`. CI do repo cobre `minimal.aip` e seeds/índices em exemplos ricos.

```bash
npm test
node packages/cli/dist/cli.js emit sql examples/blog-crud.aip
```

## Relacionado

- [Visão geral dos emitters](/pt/emitters)
- [EMITTER_OBJECTIVES mestre](https://github.com/eudameron/aiparlance/blob/main/EMITTER_OBJECTIVES.md)
- [Primeiros emitters](/pt/first-transpiler)
- [Começando](/pt/getting-started)
