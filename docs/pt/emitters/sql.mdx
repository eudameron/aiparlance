---
title: Emitter PostgreSQL
description: "@aiparlance/sql — papel schema, nota 19/23, objetivos pass/partial/fail"
---

# Emitter PostgreSQL

| | |
|---|---|
| Pacote | `@aiparlance/sql` |
| CLI | `aip emit sql` |
| Papel | `schema` |
| **Nota** | **19/23** (83%) |
| Faixa | Pronto para happy path |
| Scorecard | [transpilers/sql/EMITTER_OBJECTIVES.md](https://github.com/eudameron/aiparlance/blob/main/transpilers/sql/EMITTER_OBJECTIVES.md) |

Avaliado pelo checklist mestre ([55 IDs](https://github.com/eudameron/aiparlance/blob/main/EMITTER_OBJECTIVES.md), v2 · 2026-08-08).

## Resumo

| ✅ Pass | ⚠️ Partial | ❌ Fail | ➖ N/A |
|---|---|---|---|
| 19 | 0 | 4 | 32 |

## O que gera

DDL PostgreSQL: `CREATE TABLE`, indexes, seeds, `email` como `CITEXT`, views `{table}_active` para soft-delete e migrations init (`0001_init.up/down` via `aip emit sql --migrations`).

## ✅ Passou

| ID | Objetivo |
|---|---|
| `A1`, `A4`–`A9` | Tabelas, enums, FKs, id/timestamps, coluna soft-delete + views ativas |
| `B1`, `B2`, `B4` | NOT NULL, UNIQUE, `email` → CITEXT |
| `C1`–`C6` | DDL, indexes, seeds, migrations up/down, alvo postgres |
| `H3`–`H5` | Goldens / CI / naming |

## ❌ Ainda falhando (aplicáveis)

| ID | Objetivo |
|---|---|
| `B3` | CHECKs de `validation { }` além de UNIQUE/required |
| `C7` | Transactions para writes multi-statement / workflow |
| `H1`, `H2` | COMMENT ON / SQL de fixture |

Scorecard completo: [transpilers/sql/EMITTER_OBJECTIVES.md](https://github.com/eudameron/aiparlance/blob/main/transpilers/sql/EMITTER_OBJECTIVES.md).

## Testes

```bash
npm test
node packages/cli/dist/cli.js emit sql examples/blog-crud.aip
node packages/cli/dist/cli.js emit sql --migrations examples/blog-crud.aip
```

## Relacionados

- [Visão geral dos emitters](/pt/emitters)
- [EMITTER_OBJECTIVES mestre](https://github.com/eudameron/aiparlance/blob/main/EMITTER_OBJECTIVES.md)
- [Comece aqui](/pt/getting-started)
