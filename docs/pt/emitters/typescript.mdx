---
title: Emitter TypeScript
description: "@aiparlance/typescript — papel app, nota 36/55, objetivos pass/partial/fail"
---

# Emitter TypeScript

| | |
|---|---|
| Pacote | `@aiparlance/typescript` |
| CLI | `aip emit typescript` |
| Papel | `app` |
| **Nota** | **36/55** (65%) |
| Faixa | Fatia forte |
| Scorecard | [transpilers/typescript/EMITTER_OBJECTIVES.md](https://github.com/eudameron/aiparlance/blob/main/transpilers/typescript/EMITTER_OBJECTIVES.md) |

Avaliado pelo checklist mestre ([55 IDs](https://github.com/eudameron/aiparlance/blob/main/EMITTER_OBJECTIVES.md), v2 · 2026-08-08).

## Resumo

| ✅ Pass | ⚠️ Partial | ❌ Fail | ➖ N/A |
|---|---|---|---|
| 36 | 0 | 19 | 0 |

## O que gera

Interfaces, schemas Zod, helpers de policy e `createCrudApp()` / `listenCrudApp()` com memória ou Postgres (`DATABASE_URL`), JWT (`AIP_JWT_SECRET`), CORS, rate limits, unique→409, paginação e erros tipados. Peers: `zod`, `pg`, `jose`.

## ✅ Passou

| ID | Objetivo |
|---|---|
| `A1`–`A9` | Formas de domínio até semântica de soft-delete |
| `B1`–`B4` | required/optional, unique→409, validation, tipos semânticos |
| `D1`–`D7` | CRUD, prefix, cors, rate_limit, paginação, erros tipados, JSON |
| `E1`–`E6` | Auth JWT, policy, owner / owner_or_manager (admin\|editor), 401/403 |
| `F1`–`F5` | CRUD executável, Postgres ou memória, Zod + policy em runtime |
| `H3`–`H5` | Goldens / CI / naming |
| `J1`, `J3` | Env para DB/JWT/PORT; `/health` |

## ❌ Ainda falhando (aplicáveis)

| ID | Objetivo |
|---|---|
| `C1`–`C7` | Camada ORM/query, indexes/seeds/migrations no app, transactions |
| `G1`–`G7` | Behavior (jobs, queues, workflows, lifecycle, ai_context) |
| `H1`, `H2` | Docs humanas / scaffolds de teste |
| `I1`, `I2` | SDK de cliente / matriz completa contract↔runtime |
| `J2` | Logs estruturados / request id |

Scorecard completo: [transpilers/typescript/EMITTER_OBJECTIVES.md](https://github.com/eudameron/aiparlance/blob/main/transpilers/typescript/EMITTER_OBJECTIVES.md).

## Testes

```bash
npm test
node packages/cli/dist/cli.js emit typescript examples/blog-crud.aip
```

## Relacionados

- [Visão geral dos emitters](/pt/emitters)
- [EMITTER_OBJECTIVES mestre](https://github.com/eudameron/aiparlance/blob/main/EMITTER_OBJECTIVES.md)
- [Comece aqui](/pt/getting-started)
